// KaguneX WAProto — core Message fields (publicly documented field numbers).
// Not a full dump of WhatsApp protos. Extend via proto-extract when available.
syntax = "proto2";
package kagunex.wa;

message Message {
  optional string conversation = 1;
  optional ImageMessage imageMessage = 3;
  optional ContactMessage contactMessage = 5;
  optional LocationMessage locationMessage = 6;
  optional ExtendedTextMessage extendedTextMessage = 7;
  optional DocumentMessage documentMessage = 8;
  optional AudioMessage audioMessage = 9;
  optional VideoMessage videoMessage = 10;
  optional StickerMessage stickerMessage = 26;
  optional ReactionMessage reactionMessage = 46;
  optional ProtocolMessage protocolMessage = 12;
}

message ExtendedTextMessage {
  optional string text = 1;
  optional ContextInfo contextInfo = 17;
}

message ImageMessage {
  optional string caption = 1;
  optional string mimetype = 2;
  optional bytes mediaKey = 8;
  optional string url = 3;
  optional string directPath = 15;
}

message VideoMessage {
  optional string caption = 1;
  optional string mimetype = 2;
  optional bytes mediaKey = 8;
  optional string url = 3;
}

message AudioMessage {
  optional string mimetype = 1;
  optional bytes mediaKey = 8;
  optional bool ptt = 5;
  optional string url = 3;
}

message DocumentMessage {
  optional string mimetype = 1;
  optional string fileName = 2;
  optional bytes mediaKey = 8;
  optional string url = 3;
  optional string caption = 11;
}

message StickerMessage {
  optional string mimetype = 1;
  optional bytes mediaKey = 8;
  optional string url = 3;
}

message ContactMessage {
  optional string displayName = 1;
  optional string vcard = 16;
}

message LocationMessage {
  optional double degreesLatitude = 1;
  optional double degreesLongitude = 2;
  optional string name = 3;
  optional string address = 4;
}

message ReactionMessage {
  optional MessageKey key = 1;
  optional string text = 2;
}

message ProtocolMessage {
  optional MessageKey key = 1;
  optional int32 type = 2;
}

message MessageKey {
  optional string remoteJid = 1;
  optional bool fromMe = 2;
  optional string id = 3;
  optional string participant = 4;
}

message ContextInfo {
  optional string stanzaId = 1;
  optional string participant = 2;
  repeated string mentionedJid = 15;
  optional Message quotedMessage = 3;
}
